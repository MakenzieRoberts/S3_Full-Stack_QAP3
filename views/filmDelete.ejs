<html lang="en">
	<%- include('./partials/head.ejs') %>
	<body>
		<%- include('./partials/nav.ejs') %>
		<div class="create content">
			<form action="/films/<%= locals.id %>?_method=DELETE" method="POST">
				<br />
				<label
					>Are you sure you want to delete this film?<br /><br />
					<!-- Fix unnessecary <br>s with css -->

					<table>
						<tr>
							<th>Title</th>
							<th>Release Year</th>
							<th>Rating</th>
							<th rowspan="3">
								<button>DELETE</button>
							</th>
						</tr>

						<tr>
							<td><%= locals.title %></td>
							<td><%= locals.releaseYear %></td>
							<td><%= locals.rating %></td>
						</tr>
					</table>

					<!-- <input type="text" id="<%= locals.id %>" name="film" value="<%= locals.title %> <%= locals.releaseYear %> <%= locals.rating %>" readonly> -->
				</label>
				<!-- <button>DELETE</button> -->
			</form>
		</div>
	</body>
	<%- include('./partials/foot.ejs') %>
</html>
